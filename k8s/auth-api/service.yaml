apiVersion: v1
kind: Service
metadata:
  name: auth-api
  namespace: feature-flag
spec:
  selector:
    app: auth-api

  ports:
    - name: http
      port: 5001
      targetPort: http
      protocol: TCP

  type: ClusterIP