apiVersion: v1
kind: Service
metadata:
  name: feature-flag-ui
  namespace: feature-flag
spec:
  selector:
    app: feature-flag-ui
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP